# Хакатон по анализу данных (формат DANO)

**Дата проведения:** 13 декабря  
**Время:** 10:45 — 13:45  
**Формат:** Командная работа (2–3 человека)

---

## Описание задачи

Участникам предоставляется реальный набор данных (датасет) и один исследовательский вопрос. Ваша цель — не просто применить методы программирования, а дать аргументированный ответ на поставленный вопрос, опираясь на данные.

Вам предстоит пройти полный цикл исследования: от предобработки данных до формирования выводов.

---

## Требования к результату (Deliverables)

Итогом работы является **Jupyter Notebook** (аналитическая записка), который должен представлять собой связный рассказ с логической структурой.

**Структура работы:**
1.  **Постановка задачи:** Краткое описание того, что мы ищем.
2.  **Предобработка данных:** Очистка, работа с пропусками и типами данных.
3.  **Исследовательский анализ (EDA):**
    *   Построение графиков и расчет статистик.
    *   **Важно:** Каждая визуализация или таблица должна сопровождаться текстовым комментарием (интерпретацией). Графики без выводов не оцениваются.
4.  **Итоговый вывод:** Четкий, обоснованный ответ на исследовательский вопрос.

---

## Сдача работы

По окончании работы участники должны загрузить свой файл в формате `.ipynb` в Google Форму.

👉 **[Ссылка на Google Форму для отправки решения](https://forms.gle/3LbzywJUXaNSPkY19)**

---

## Критерии оценивания

Максимальная оценка — **10 баллов**.

Оценивается качество аналитической работы и умение доносить информацию:
*   **Техническая часть:** Корректность кода, отсутствие ошибок, правильная работа с данными.
*   **Визуализация:** Читаемость графиков (наличие подписей осей, названий, легенд).
*   **Интерпретация:** Полнота текстового пояснения к полученным результатам (сторителлинг).
*   **Ответ на вопрос:** Насколько обоснован и понятен финальный вывод.

---

## Использование ИИ и Академическая честность

Работа должна быть выполнена участниками команды в отведенное время.

**1. Нейросети разрешены:**
Я разрешаю использование инструментов на базе ИИ (ChatGPT, Claude, GitHub Copilot и др.) для генерации кода, поиска идей для визуализации или отладки ошибок. Это современные инструменты аналитика.

**2. Ответственность за результат:**
Вы должны полностью понимать код, который используете в своем решении. Слепое копирование ответов нейросети без проверки логики может привести к фактическим ошибкам, за которые будут снижены баллы. Если я задаю вопрос по коду, вы должны уметь объяснить, как он работает.

**3. Запрет на плагиат:**
Категорически **запрещается** заимствование готовых решений (кода или текста) у других команд.

> **Внимание:**
> В случае обнаружения списывания у другой команды, работа аннулируется. Участники дисквалифицированной команды лишаются оценки за хакатон, а в ЭлЖур выставляется оценка **1**. 🙂

---

## Данные и задача

Вам представлен датасет реальных заказов в Тинькофф Афиша (небольшой сэмпл по пользователям). Каждая строка — это один заказ, сделанный в какой-то день (creation_date) на какой-то день (session_date).

### Описание данных:

| Поле | Описание |
| :--- | :--- |
| `order_id` | уникальный ключ заказа |
| `creation_date` | дата создания заказа |
| `session_date` | дата сеанса |
| `movie_id` | (id фильма) |
| `movie_name` | название фильма |
| `movie_duration` | продолжительность фильма (иногда текстом) |
| `movie_age_restriction` | возрастные ограничения на фильм (заполняется партнерами) |
| `movie_rating` | рейтинг фильма (нечто среднее imdb и кинопоиска) |
| `genre_is_triller` | булево (1 или 0) на жанр — триллер |
| `genre_is_military` | булево (1 или 0) на жанр — военный |
| `genre_is_action` | булево (1 или 0) на жанр — боевик / экшн |
| `genre_is_biographical` | булево (1 или 0) на жанр — биография |
| `genre_is_fairy_tale` | булево (1 или 0) на жанр — сказка |
| `genre_is_science` | булево (1 или 0) на жанр — научный |
| `genre_is_fantasy` | булево (1 или 0) на жанр — фэнтези |
| `genre_is_historical` | булево (1 или 0) на жанр — исторический |
| `genre_is_drama` | булево (1 или 0) на жанр — драмма |
| `genre_is_animation` | булево (1 или 0) на жанр — анимация |
| `genre_is_cartoon` | булево (1 или 0) на жанр — мультяшный |
| `genre_is_documentary` | булево (1 или 0) на жанр — документальный |
| `genre_is_adventure` | булево (1 или 0) на жанр — приключение |
| `genre_is_fiction` | булево (1 или 0) на жанр — фантастика |
| `genre_is_childish` | булево (1 или 0) на жанр — детский |
| `genre_is_horror` | булево (1 или 0) на жанр — ужастик |
| `genre_is_humor` | булево (1 или 0) на жанр — юмор |
| `genre_is_western` | булево (1 или 0) на жанр — вестерн |
| `genre_is_noir` | булево (1 или 0) на жанр — ноар |
| `genre_is_detective` | булево (1 или 0) на жанр — детектив |
| `genre_is_biography` | булево (1 или 0) на жанр — биография |
| `genre_is_criminal` | булево (1 или 0) на жанр — криминалл |
| `genre_is_tragicomedy` | булево (1 или 0) на жанр — трагикомедия |
| `genre_is_biopic` | булево (1 или 0) на жанр — байопик |
| `genre_is_mystic` | булево (1 или 0) на жанр — мистика |
| `genre_is_family` | булево (1 или 0) на жанр — семейный |
| `genre_is_comedy` | булево (1 или 0) на жанр — комедия |
| `genre_is_arthouse` | булево (1 или 0) на жанр — артхаус |
| `count as number_of_tickets`| кол-во билетов в чеке |
| `places` | комментарий по ряду и месту (указаны места из билета) |
| `sales_in_cu` | выручка за заказ в условных единицах |
| `ticket_price_in_cu` | стоимость одного билета в условных единицах |
| `cinema_name` | название кинотеатра |
| `cinema_address` | адрес кинотеатра |
| `cinema_latitude` | широта положения кинотеатра |
| `cinema_longitude` | долгота положения кинотеатра |
| `cinema_city` | город кинотеатра |
| `client_id` | уникальный id клиента |

### Предлагаемый вопрос для начала изучения:

Найти рецепт успешного фильма. Какими характеристиками обладает этот фильм? Попробуйте научиться предугадывать объем продаж билетов по характеристикам фильмов


### Где взять датасет

Если вы работаете в **Google Colab**, используйте следующий код:

```python
import pandas as pd
import requests
import os

# 1. Ссылка на файл (вставьте актуальную ссылку внутри кавычек)
url = "https://dano.hse.ru/mirror/pubs/share/796178083" # <------- ЭТО ССЫЛКА НА ДАТАСЕТ
output_file = "dataset.rar"

# 2. Скачивание файла
print("Скачиваю архив...")
response = requests.get(url)
with open(output_file, "wb") as f:
    f.write(response.content)
print("Скачивание завершено.")

# 3. Распаковка RAR архива (работает в среде Linux/Colab)
# Убедимся, что установлена утилита unrar
!apt-get install unrar
# Распаковываем
!unrar x -y {output_file}

df = pd.read_csv('cinema_successful_orders.csv', delimiter=';')
df.head()
```

Итого в переменной `df` должен оказаться валидный датасет

---

Удачи!
